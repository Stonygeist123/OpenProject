// import React, { createContext, useContext, useState } from "react";

// const ctx = createContext<{ theme: "dark" | "light"; readonly toggle?: () => void }>({ theme: "dark" });
// const useTheme = (): ["dark" | "light", React.Dispatch<React.SetStateAction<"dark" | "light">>] => {
//   const themeCtx = useContext<"dark" | "light">(ctx);
//   return [themeCtx, setTheme];
// };

// export default useTheme;

// eslint-disable-next-line import/no-anonymous-default-export
export default 1;
